import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 20
  },
  p: {
    marginBottom: 20,
    textAlign: 'center'
  }
});

export default styles;
