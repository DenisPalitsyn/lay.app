import React from "react";
export const RootNavigationContext = React.createContext({rootNavigation: null});
export const UserContext = React.createContext({profileUid: '', isUser: true});
export const ProfileContext = React.createContext({});
